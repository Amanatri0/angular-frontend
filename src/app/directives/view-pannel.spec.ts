import { ViewPannel } from './view-pannel';

describe('ViewPannel', () => {
  it('should create an instance', () => {
    const directive = new ViewPannel();
    expect(directive).toBeTruthy();
  });
});
